import React from 'react';
import SideBar from "../components/SideBar";

const Home = () => (
            <div className="homepage">
                HOME
                <SideBar/>
            </div>
        );

export default Home;
