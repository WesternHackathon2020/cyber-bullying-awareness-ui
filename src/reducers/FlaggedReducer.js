const initialStateFlaggedReducer = {
    flagged: [],
};

export default (state = initialStateFlaggedReducer, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
